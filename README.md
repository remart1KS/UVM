🧩 Учебная виртуальная машина (УВМ), вариант 16
📋 Ассемблер и интерпретатор
Данный проект реализует ассемблер и интерпретатор для учебной виртуальной машины (УВМ), вариант 16. Ассемблер преобразует текстовую программу в бинарный машинный код, а интерпретатор выполняет этот код, эмулируя работу виртуальной машины и формируя дамп памяти.

📁 Структура проекта
text
.
├── uvm_asm.py          # Ассемблер (текст → бинарный код)
├── uvm_interp.py       # Интерпретатор (выполнение бинарного кода)
├── uvm_ui.py          # GUI приложение
├── test_spec.asm      # Пример входной программы (тест из спецификации)
├── simple.asm         # Пример входной программы (простая)
├── full_test.asm      # Пример входной программы (полная)
├── test.bin          # Скомпилированный бинарный файл (тест)
├── simple.bin        # Скомпилированный бинарный файл (простой)
├── dump.csv          # Дамп памяти
└── README.md         # Документация проекта
⚙️ Спецификация УВМ (вариант 16)
Общие характеристики
Тип архитектуры: стековая

Формат команд: 3 байта / 1 байт (команды переменной длины)

Кодирование: little-endian

Память: 1024 ячейки (объединенная)

Команды УВМ
1️⃣ Загрузка константы — load_const
text
A B
Биты 0—3 Биты 4—18
0 Константа
Размер команды: 3 байта

Код операции (A): 0

Поле B: 15-битная константа (0..32767)

Действие: загружает константу на вершину стека

Тест (A=0, B=460): 0xC0, 0x1C, 0x00

2️⃣ Чтение значения из памяти — read_value
text
A B
Биты 0—3 Биты 4—17
2 Смещение
Размер команды: 3 байта

Код операции (A): 2

Поле B: 14-битное смещение (0..16383)

Действие: читает значение из памяти по адресу (адрес с вершины стека + смещение), результат на стек

Тест (A=2, B=1000): 0x82, 0x3E, 0x00

3️⃣ Запись значения в память — write_value
text
A
Биты 0—3
5
Размер команды: 1 байт

Код операции (A): 5

Действие: берет значение с вершины стека, затем адрес со стека, записывает значение в память

Тест (A=5): 0x05

4️⃣ Унарная операция — bitreverse
text
A B
Биты 0—3 Биты 4—17
11 Смещение
Размер команды: 3 байта

Код операции (A): 11

Поле B: 14-битное смещение (0..16383)

Действие: берет адрес с вершины стека, читает значение из памяти, выполняет побитовое обращение, записывает результат в память по адресу (адрес + смещение)

Тест (A=11, B=255): 0xFB, 0x0F, 0x00

🧪 Формат входной программы
Входной файл — обычный текстовый файл с расширением .asm.
Одна команда — одна строка.

Пример (test_spec.asm):

asm
load_const 460
read_value 1000
write_value
bitreverse 255
Пример (simple.asm):

asm
load_const 50
load_const 123
write_value
load_const 50
read_value 0
🛠 Ассемблер — uvm_asm.py
Запуск:

bash
python uvm_asm.py -i test_spec.asm -o test.bin -t 1
Параметры:

-i, --input: путь к исходному файлу с текстом программы

-o, --output: путь к двоичному файлу-результату

-t, --test: режим тестирования (0 - выкл, 1 - вкл)

Пример вывода (режим тестирования):

text
IR: [('load_const', 460), ('read_value', 1000), ('write_value',), ('bitreverse', 255)]
Bytecode: 0xC0 0x1C 0x00 0x82 0x3E 0x00 0x05 0xFB 0x0F 0x00
10
▶️ Интерпретатор — uvm_interp.py
Запуск:

bash
python uvm_interp.py -i simple.bin -o dump.csv -r 0-100
Параметры:

-i, --input: путь к бинарному файлу с ассемблированной программой

-o, --output: путь к файлу, куда будет сохранен дамп памяти

-r, --range: диапазон адресов памяти для вывода дампа

🖥️ GUI приложение — uvm_ui.py
Запуск:

bash
python uvm_ui.py
Интерфейс:

Редактируемое окно программы на языке ассемблера

Окно вывода дампа памяти

Кнопка для ассемблирования и запуска интерпретатора

📄 Формат дампа памяти
csv
Address,Value
0,0
1,0
2,0
...
50,123
...
🎓 Назначение проекта
Учебный проект для изучения принципов работы виртуальных машин, ассемблеров и интерпретаторов.

Вариант: 16
Язык: Python
